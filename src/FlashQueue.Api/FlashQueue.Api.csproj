<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <!-- Librería de endpoints minimal API + rate limiting, sin Program.cs propio: la hospeda
         FlashQueue.Workers en un único proceso (ver ADR 0013). FrameworkReference explícita
         porque este proyecto ya no usa el SDK Web, que la traía implícita. -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\FlashQueue.Application\FlashQueue.Application.csproj" />
  </ItemGroup>

</Project>
